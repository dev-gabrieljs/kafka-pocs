# === Configura��o do Kafka Broker ===
spring.kafka.bootstrap-servers=localhost:9092
# Define o endere�o do Kafka que a aplica��o usar� para se conectar.
# Aqui, aponta para o Kafka rodando localmente na porta 9092.

# === Configura��o do Consumidor Kafka ===
spring.kafka.consumer.group-id=test-group
# Define o identificador do grupo de consumidores. Consumidores com o mesmo group ID compartilham o consumo das mensagens.

spring.kafka.consumer.auto-offset-reset=earliest
# Define o comportamento quando o offset n�o � encontrado:
# "earliest" come�a a consumir desde o in�cio do t�pico;
# "latest" consome apenas novas mensagens.

# === Serializadores do Produtor ===
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
# Define como as chaves e valores das mensagens ser�o serializados ao enviar para o Kafka.
# Aqui, tudo � tratado como string.

# === Desserializadores do Consumidor ===
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
# Define como as mensagens recebidas ser�o convertidas de volta (desserializadas).
# Tamb�m tudo como string.
